import React from 'react'
import Button from '@material-ui/core/Button';

function cButton({Label}) {
    return <div> 
        <Button variant="contained" color="primary" > sdfsdf </Button>
    </div>
}

export default cButton;
